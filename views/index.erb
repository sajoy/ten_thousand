<div class="col-md-3">

  <h1>10,000</h1>
  <p> hours, projects, moments of failure. <br/>
      doubts, confused days, frustrated nights. <br/>
      second guesses, "I'm not good enough"s, <br/>
      too harsh self-judgements. <br/>
      seconds of understanding, moments of clarity, </br>
      wishes, tries, searches, lessons, <br/>
      and 10,000 more.

  </p>

  <% if @entries.any? %>
  <h3>Most Recent Entries</h3>
  <div class="timeline">
    <ul>
      <% @entries.each do |entry| %>
        <li>
          <a href="/skill/<%= entry.skill.id %>">
            <%= entry.date %>, <%= entry.skill.name %> > <% if entry.category %><%= entry.category.name %> > <% end %> <%= entry.title %>
          </a>
        </li>
      <% end %>
    </ul>
  </div>
  <% end %>

</div>

<div class="col-md-9">
  <div class="skill add">

    <p> Add a new skill to track! </p>
    <form action="/" method="post">
      <input type="text" name="name" size="15">
      <button type="submit" class="btn btn-xs btn-success glyphicon glyphicon-ok"></button>
    </form>

  </div>

  <% @skills.each do |skill| %>

    <div class="skill">

      <h2><a href="/skill/<%= skill.id %>"><%= skill.name %></a></h2>

    </div>

  <% end %>

</div>
